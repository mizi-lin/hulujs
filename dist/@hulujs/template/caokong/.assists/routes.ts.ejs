/**
 * 路由配置
 */
const routes = <%- JSON.stringify(routes, function(key, value) {
    if (typeof value === 'function') {
      return `__FUNC_PLACEHOLDER_START__${value.toString().replace(/\n/g, '')}__FUNC_PLACEHOLDER_END__`
    }
    return value;
}, 2).replace(/\"__FUNC_PLACEHOLDER_START__/g, '').replace(/__FUNC_PLACEHOLDER_END__\"/g, '') %>;

export default routes;