import { Params } from '@hulu/types';
declare function format(value: string, format: Params, nullInstead?: string): string;
declare function format(number: string, format: string, nullInstead?: string): string;
export default format;
