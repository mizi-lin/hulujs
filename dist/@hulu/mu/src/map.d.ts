import { IterateeCollection, MapIteratee } from './each.js';
declare const map: (collect: IterateeCollection, iteratee: MapIteratee, initialValue?: Record<string, any> | Record<string, any>[]) => any;
export default map;
